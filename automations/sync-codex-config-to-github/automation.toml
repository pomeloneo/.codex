version = 1
id = "sync-codex-config-to-github"
kind = "heartbeat"
name = "Sync Codex config to GitHub"
prompt = "Synchronize the local Codex configuration Git repository at /Users/bytedance/.codex to its configured origin. Run the sync conservatively: verify it is a Git repository, inspect status, stage all non-ignored changes with git add -A, commit only if there are staged changes using the message \"Auto-sync Codex config\", pull/rebase from origin/main if needed, then push main to origin. Respect the repository's existing .gitignore and do not force-add ignored credentials, local state, history, logs, caches, or database files. Do not print secret file contents or credential values. Report whether a commit was created, whether push succeeded, and any actionable failure."
status = "ACTIVE"
rrule = "FREQ=MINUTELY;INTERVAL=30"
target_thread_id = "019e8b92-2124-7c61-816b-4960e356232b"
created_at = 1780458336784
updated_at = 1780458336784
